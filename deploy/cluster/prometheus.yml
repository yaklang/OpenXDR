global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/openxdr-alerts.yml

scrape_configs:
  - job_name: openxdr
    static_configs:
      - targets: [server1:8080, server2:8080, server3:8080]
  - job_name: nats
    static_configs:
      - targets: [nats-exporter:7777]

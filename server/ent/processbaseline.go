// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"openxdr/server/ent/processbaseline"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// ProcessBaseline is the model entity for the ProcessBaseline schema.
type ProcessBaseline struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// AssetID holds the value of the "asset_id" field.
	AssetID uuid.UUID `json:"asset_id,omitempty"`
	// ExePath holds the value of the "exe_path" field.
	ExePath string `json:"exe_path,omitempty"`
	// FirstSeen holds the value of the "first_seen" field.
	FirstSeen    time.Time `json:"first_seen,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProcessBaseline) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case processbaseline.FieldExePath:
			values[i] = new(sql.NullString)
		case processbaseline.FieldFirstSeen:
			values[i] = new(sql.NullTime)
		case processbaseline.FieldID, processbaseline.FieldAssetID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProcessBaseline fields.
func (_m *ProcessBaseline) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case processbaseline.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case processbaseline.FieldAssetID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field asset_id", values[i])
			} else if value != nil {
				_m.AssetID = *value
			}
		case processbaseline.FieldExePath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field exe_path", values[i])
			} else if value.Valid {
				_m.ExePath = value.String
			}
		case processbaseline.FieldFirstSeen:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field first_seen", values[i])
			} else if value.Valid {
				_m.FirstSeen = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProcessBaseline.
// This includes values selected through modifiers, order, etc.
func (_m *ProcessBaseline) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ProcessBaseline.
// Note that you need to call ProcessBaseline.Unwrap() before calling this method if this ProcessBaseline
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProcessBaseline) Update() *ProcessBaselineUpdateOne {
	return NewProcessBaselineClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProcessBaseline entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProcessBaseline) Unwrap() *ProcessBaseline {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProcessBaseline is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProcessBaseline) String() string {
	var builder strings.Builder
	builder.WriteString("ProcessBaseline(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("asset_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AssetID))
	builder.WriteString(", ")
	builder.WriteString("exe_path=")
	builder.WriteString(_m.ExePath)
	builder.WriteString(", ")
	builder.WriteString("first_seen=")
	builder.WriteString(_m.FirstSeen.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProcessBaselines is a parsable slice of ProcessBaseline.
type ProcessBaselines []*ProcessBaseline
